[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "osu_pp"
version = "0.1.0"
description = "osu! local PP calculator (osu!, mania, taiko) via Rust"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
